import { Poppins } from "next/font/google";

export const poppins = Poppins({
    weight: "600",
    subsets: ["latin"],
  });

export const poppinsmedium = Poppins({
    weight: "500",
    subsets: ["latin"],
  });
